
#the function will concat the 2 strings
def concatString(name, lastName):

  #this 2 variables will stored the value from the parameters
  name = name;
  lastName = lastName;

  #the function returns the 2 strings concatenated
  return name + ' ' + lastName;

#as usual this mf prints the value returned
print(concatString('Jorge', 'Ramos Gonzalez'));

#and this other fella prints the 2 concatenated numbers
print(10, 10);